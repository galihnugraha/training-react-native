import { Dimensions } from "react-native";

export const WIDTH = Dimensions.get('window').width
export const HEIGHT = Dimensions.get('window').height
export const HEIGHTLOGIN = Dimensions.get('screen').height
export const SHADOW = {
  shadowColor: "#000",
  shadowOffset: {
    width: 0,
    height: 6,
  },
  shadowOpacity: 0.37,
  shadowRadius: 7.49,

  elevation: 12,
}